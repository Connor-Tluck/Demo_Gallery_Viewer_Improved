// DEMO_API_KEY comes from keys.js (NEARMAP_KEY) - keys.js must be loaded first
var DEMO_API_KEY = (typeof NEARMAP_KEY !== "undefined" ? NEARMAP_KEY : "");

var CENTER = [-74.00602, 40.71451];

var MIN_ZOOM = 0;

var MAX_ZOOM = 24;

var ZOOM = 13;
