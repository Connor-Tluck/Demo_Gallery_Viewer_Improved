# Netlify build configuration
# API keys: Set MAPBOX_PUBLIC_TOKEN, GOOGLE_MAPS_API_KEY, CESIUM_ION_ACCESS_TOKEN, NEARMAP_API_KEY in Netlify env vars

[build]
  command = "npm run build"
  publish = "."

# keys.js is generated from env vars during build
